# Core Flask web framework
Flask==3.1.1

# HTTP requests for API calls (ElevenLabs, downloading files)
requests==2.32.3

# Environment variables management
python-dotenv==1.0.0

# AWS SDK for S3 uploads
boto3==1.34.162

# Date/time utilities (included with boto3)
python-dateutil==2.8.2

# SadTalker dependencies (install separately):
# - Follow SadTalker repository installation instructions
# - Requires PyTorch, torchvision, and other ML dependencies
# - May need CUDA for GPU acceleration
# - See: https://github.com/OpenTalker/SadTalker

# Built-in modules used (no installation needed):
# - os, sys, subprocess, tempfile, datetime, shutil, uuid
